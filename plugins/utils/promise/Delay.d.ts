/**
 * Resolve a promise after a delay.
 *
 * @param time - Delay time in milliseconds.
 * @param result - Result value to resolve with.
 * @returns Promise resolved after the delay.
 */
export default function (
    time: number,
    result?: any
): Promise<any>;
