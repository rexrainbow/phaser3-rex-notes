/**
 * Decrypt an XOR-encrypted string with a password.
 *
 * @param data - Encrypted string data.
 * @param pwd - Password string.
 * @returns Decrypted string.
 */
export default function Decrypt(
    data: string,
    pwd: string
): string;
