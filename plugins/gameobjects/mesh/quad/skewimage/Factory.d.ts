import SkewImage from './SkewImage';

export default function (
    x?: number,
    y?: number,
    key?: string,
    frame?: string | null
): SkewImage;

export default function (
    config?: SkewImage.IConfig
): SkewImage;
