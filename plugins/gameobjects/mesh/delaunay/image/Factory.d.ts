import Image from './Image';

export default function (
    x?: number, y?: number,
    key?: string, frame?: string,
    config?: Image.IConfig
): Image;

export default function (
    config?: Image.IConfig
): Image;
