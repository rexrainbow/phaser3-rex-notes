import Quad from './Quad';

export default function (
    x?: number,
    y?: number,
    width?: number,
    height?: number,
    fillColor?: number,
    fillAlpha?: number
): Quad;

export default function (
    config?: Quad.IConfig
): Quad;