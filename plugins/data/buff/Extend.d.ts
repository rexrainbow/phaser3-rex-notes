import DataManager from './DataManager';

/**
 * Extend a Phaser data manager with buff-related APIs.
 *
 * @param dataManager - Source data manager to extend.
 * @returns Extended data manager with buff support.
 */
export default function Extend(
    dataManager: Phaser.Data.DataManager
): DataManager;
