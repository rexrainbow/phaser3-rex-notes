```
<document>
    ownerID - ID of owner, assume that each owner has an unique ID
    fileName - Unique file name of each owner
    type - 'header' for indexing (required), or 'content' (optional)
    _contentID - document ID of content, stored in header only
    ... 
```