import SingleLineInput from './SingleLineInput';

export default function (
    x?: number, y?: number,
    fixedWidth?: number, fixedHeight?: number,
    config?: SingleLineInput.IConfig
): SingleLineInput;