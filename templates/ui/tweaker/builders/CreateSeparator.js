import CreateBackground from '../../utils/build/CreateBackground.js';

var CreateSeparator = function (scene, config, style) {
    return CreateBackground(scene, style);
}

export default CreateSeparator;