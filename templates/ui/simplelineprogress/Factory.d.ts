import SimpleLineProgress from './SimpleLineProgress';

export default function (
    config?: SimpleLineProgress.IConfig
): SimpleLineProgress;

export default function (
    x?: number, y?: number,
    radius?: number,
    barColor?: string | number,
    value?: number,
    config?: SimpleLineProgress.IConfig
): SimpleLineProgress;