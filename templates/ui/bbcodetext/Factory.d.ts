import BBCodeText from './BBCodeText';
import { TextStyle } from '../../../plugins/bbcodetext';

export default function (
    x?: number, y?: number,
    content?: string,
    style?: TextStyle
): BBCodeText;